{
  "name": "pokedex",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "dependencies": {
    "express": "4.18.2",
    "swagger-ui-express": "3.0.10"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.11.9",
    "@types/swagger-ui-express": "^4.1.3",
    "nodemon": "^2.0.20",
    "prettier": "2.8.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "start": "npm run pre-commit && node dist/index.js",
    "dev": "  nodemon src/index.ts",
    "build": "npm run pre-commit && tsc -p ./",
    "test": "npm run pre-commit && echo \"Error: no test specified\" && exit 1",
    "pre-commit": "copy pre-commit .git/hooks/pre-commit || exit 0 && cp pre-commit .git/hooks/pre-commit || exit 0 && chmod 775 .git/hooks/pre-commit",
    "format": "prettier --ignore-path .gitignore node_modules build --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
